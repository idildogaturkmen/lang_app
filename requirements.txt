# Core Streamlit and Data Processing
streamlit==1.28.1
numpy==1.24.3
pandas==2.0.3
Pillow==9.5.0

# TensorFlow CPU-only (memory optimized)
tensorflow-cpu==2.13.0
tensorflow-hub==0.14.0

# Other core dependencies
matplotlib==3.7.2
opencv-python-headless==4.8.0.76
psutil==5.9.5

# Translation and text-to-speech
deep-translator==1.11.4
gtts==2.3.2

# Basic utilities
requests==2.31.0

# REMOVED: sqlite3 (this is built-in to Python, not a pip package)
# REMOVED: base64, io, time, datetime, re, queue, concurrent.futures, 
#          functools, hashlib, collections, random (all built-in modules)

# Optional: Pronunciation Practice (comment out to save memory)
python-Levenshtein==0.21.1
streamlit-webrtc==0.47.1
av==10.0.0
SpeechRecognition==3.10.0
soundfile==0.12.1
librosa==0.10.1
pydub==0.25.1