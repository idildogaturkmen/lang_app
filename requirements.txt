# Core dependencies - optimized for memory
streamlit==1.28.1
numpy==1.24.3
pandas==2.0.3
Pillow==9.5.0

# TensorFlow CPU-only (much smaller memory footprint)
tensorflow-cpu==2.13.0
tensorflow-hub==0.14.0

# Other core dependencies
matplotlib==3.7.2
opencv-python-headless==4.8.0.76
psutil==5.9.5

# Translation and language processing
deep-translator==1.11.4
gtts==2.3.2

# Database
sqlite3

# Additional utilities (lightweight versions)
requests==2.31.0
base64
io
time
datetime
sqlite3
re
queue
concurrent.futures
functools
hashlib
collections
random

# Optional: If you need these for other features
streamlit-webrtc==0.45.0  # Only if using pronunciation practice
speech-recognition==3.10.0  # Only if using pronunciation practice
librosa==0.10.1  # Only if using pronunciation practice